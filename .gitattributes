# Normalise line endings so a Windows checkout does not commit CRLF.
* text=auto

# These must stay LF or they break when run on Linux.
*.sh text eol=lf
*.in text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Notebooks and data are not diffed.
*.ipynb -diff
*.parquet binary
